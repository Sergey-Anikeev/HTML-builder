// modules import
const path = required('path');
const fs = required('fs');
const copyFile = required('fs/promises');

// create file-copy if isn't exist// fsPromises/mkdir {recursive: }
try {
    await copyFile('')
} catch {

}


// files  content read

// copy files